# ADHD 生產力系統 - SSL/HTTPS 配置範例
# 複製此文件為 .env 並填入實際值

# ===================================================
# SSL 配置
# ===================================================

# SSL 模式 (development|production)
SSL_MODE=development

# 生產環境域名配置
DOMAIN=your-domain.com
CERTBOT_EMAIL=admin@your-domain.com

# ===================================================
# 基本系統配置
# ===================================================

# 資料庫配置
POSTGRES_DB=adhd_productivity
POSTGRES_USER=adhd_user
POSTGRES_PASSWORD=adhd_secure_pass_2024

# Redis 配置
REDIS_PASSWORD=redis_secure_pass_2024

# JWT 配置
JWT_SECRET_KEY=your-super-secret-jwt-key-with-32-characters-minimum
JWT_ISSUER=ADHDProductivitySystem
JWT_AUDIENCE=ADHDUsers
JWT_EXPIRY_MINUTES=15

# 應用配置
ASPNETCORE_ENVIRONMENT=Production
NODE_ENV=production

# 前端配置
VITE_API_BASE_URL=https://your-domain.com/api
VITE_SIGNALR_HUB_URL=https://your-domain.com/hubs

# ===================================================
# 監控配置
# ===================================================

# Grafana 配置
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=secure_grafana_password
GRAFANA_SECRET_KEY=grafana_secret_key_32_characters

# pgAdmin 配置
PGADMIN_DEFAULT_EMAIL=admin@your-domain.com
PGADMIN_DEFAULT_PASSWORD=secure_pgadmin_password

# ===================================================
# 安全配置
# ===================================================

# CORS 允許的來源 (生產環境)
AllowedOrigins__0=https://your-domain.com
AllowedOrigins__1=https://www.your-domain.com

# 日誌等級
LOG_LEVEL=Information

# ===================================================
# 使用說明
# ===================================================

# 開發環境設置:
# 1. 複製此文件為 .env
# 2. 設置 SSL_MODE=development
# 3. 執行: ./security/scripts/ssl-manager.sh dev
# 4. 啟動: docker-compose up -d

# 生產環境設置:
# 1. 複製此文件為 .env
# 2. 設置 SSL_MODE=production
# 3. 填入正確的 DOMAIN 和 CERTBOT_EMAIL
# 4. 確保域名 DNS 指向服務器
# 5. 執行: ./security/scripts/ssl-manager.sh prod
# 6. 啟動: docker-compose --profile production up -d